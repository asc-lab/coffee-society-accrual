server:
  port: 9004

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: admin
      password: admin
      database: admin
    elasticsearch:
      cluster-name: docker-cluster
      cluster-nodes: localhost:9300

logging:
  level:
    org:
      springframework: DEBUG


security:
  oauth2:
    resource:
      id: accrual-rest-api
      jwt:
        key-value: secretKey

#axon:
#  eventhandling:
#    processors:
#      queryModel:
#        mode: tracking
#        source: eventBus